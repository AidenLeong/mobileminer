class Product {
  String id, name, price, quantity, type, os, battery, sensor, memory, weight ;
  Product({
    this.id,
    this.name,
    this.price,
    this.quantity,
    this.type,
    this.os,
    this.battery,
    this.sensor,
    this.memory,
    this.weight,
  });
}